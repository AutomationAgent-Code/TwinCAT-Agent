# PLC 正文交付策略

plc_read / plc_read_smart / plc_read_fast / plc_read_current 已返回的声明、实现和成员正文
不再经过通用结果裁剪或强制二次分页；界面和上下文历史投影保留原文。
单对象 live COM 读取默认 max_lines=0（完整），显式正数仍表示用户选择分页。
批量请求自身的总量/分页限制仍存在，返回的分页元数据必须保留，不冒充全量。

此策略不扩大模型提供商的上下文窗口，也不取消整个历史轮次的预算淘汰。
已经被旧版裁掉的正文不能凭空恢复，需要修复生效后重新读取。
不修改 PLC、模型配置或运行时状态。
