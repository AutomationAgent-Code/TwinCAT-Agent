# TwinCAT Agent v1.0.8.58

- 跨会话 `task` 改由后端直接调度，目标会话无需打开或被选中即可执行。
- 目标会话打开时实时显示 Master 指令、工具调用和执行结果。
- 普通 `main/chat` 会话可被原子领取为后台任务，避免重复执行。
- 后台任务完成后自动刷新目标会话上下文，防止后续输入覆盖执行历史。
- 思考内容默认折叠为“思考过程（点击展开）”。
