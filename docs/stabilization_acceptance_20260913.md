# 稳定化验收台账（2026-09-13）

状态：本轮源码整合和离线回归已完成；尚未发布安装版，不修改业务 PLC。

## 收尾结果

- 完整回归：2295 passed / 117 subtests；随后新增父目录失败测试，相关24项通过。
- 删除/重命名刷新父COM对象、函数完整签名共享、成员基线唯一实时路径解析均已实施。
- 显式父目录无法解析时在 CreateChild 前报错，注明未创建；不自动换目录。
- 三个离线契约测试补齐了真实登录状态证据；目录事件hub测试补齐新委托初始化，
  不关闭编译告警；Git/编辑器错误码按状态、冲突和身份分别登记。
- 实时只读验收：PID15232 / TwinCAT Project2，FB_Demo_Fb 成员基线成功读取，
  仅有 Reset，无 IsIdle；确认历史删除已生效，不再重放删除。
- 未执行真实创建、改写、删除、编译或运行时动作。完整现场写入链不宣称已验收。

## 本轮证据

- 模拟旧父 COM wrapper 在删除后仍枚举失效属性，覆盖 property/Get 及后续方法删除。
- 删除/重命名后按原精确路径重新定位父对象；身份变化、回读失败或树不匹配仍为 uncertain。
- 不重放 DeleteChild，不通过忽略异常/丢弃节点制造成功。
- 成员、dirty 基线、模板和前置门禁相关 57 项测试通过。
- 全量初跑 2280 passed / 5 failed / 117 subtests；其中 3 个离线策略相关测试
  缺少真实新增契约要求的 mocked PLC 登录证据，已补齐并通过专项回归。
- 剩余失败：ShellInteractionHub 的 OpenDirectoryRequested 新字段在独立测试编译中
  触发未赋值警告；新 git/source-editor 错误类型与 git_repository 条件未全部登记。
  不删除测试，不降低这些检查的要求；需结合相关工作区变更继续整合。

## 尚未验收

- 前述源码及模拟回归已完成，完整现场验证仍待安排。
- 隔离测试对象上的真实创建→读取→修改→删除→回读→编译闭环。
- 安装版文件清单、配置保护、回滚与服务验证。

模拟回归不证明实际 XAE 的 COM 缓存必然刷新；需现场测试，不提前承诺问题已消失。
