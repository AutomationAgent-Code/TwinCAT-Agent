Initialize := FALSE;
CASE DescendantSequenceState OF
    0:
        RegisterComponent(Component := {{AXIS_VAR}});
        {{AXIS_VAR}}.Velocity   := {{VELOCITY}};
        DescendantSequenceState := DescendantSequenceState + 10;

    10: // 调基类初始化全部已注册的子模块/组件
        IF SUPER^.Initialize() THEN
            DescendantSequenceState := DescendantSequenceState + 10;
        END_IF

    20:
        Initialize := TRUE;
END_CASE
