(*
    自定义回零例程。挂到轴上：Axis.HomeMethod := <本实例>; 然后 Axis.Home()。

    分工（照抄 SPT 官方样例的写法）：
      Execute()     只负责「接不接这条命令」，接了就置 _Busy 并起序列，立刻返回
      CyclicLogic() 每周期跑，MC 块在这里被反复调用直到 Done/Error

    为什么不能把 MC 块写进 Execute：MC 功能块靠每周期调用推进内部状态机，
    只在命令那一拍调一次，轴会一直被占住。
*)
