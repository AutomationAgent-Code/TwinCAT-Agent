(*
 * 功能：运动命令契约 —— 一次可执行的轴动作（定位/回零/停止…）
 * 作者：fblib / 版本：2.0
 * 说明：控制器只认这个接口，不关心具体是哪种运动。新增运动方式 = 新写一个实现类，
 *       控制器无需改动（开闭原则）。生命周期：Start() → 每周期 Update() 直到返回
 *       TRUE（结束）→ 读 Done/Error 判定结果。
 *)
INTERFACE I_MotionCommand
